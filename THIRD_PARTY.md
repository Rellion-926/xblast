# Third Party

This package currently depends on:

- Qualcomm QAIRT/QNN headers and runtime libraries.
- QNN context binaries generated by the existing AOT toolchain.
- nlohmann/json, used by the Qwen3 tokenizer loader.

The runtime loads QNN libraries dynamically on device. Generated context
binaries are deployment artifacts and are not source dependencies.
